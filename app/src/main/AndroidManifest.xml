<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ShowAccount"></activity>
        <activity android:name=".activities.ResetPassword" />
        <activity android:name=".activities.TransferOtherAccount" />
        <activity android:name=".activities.TransferOwnAccount" />
        <activity android:name=".activities.AccountMenu" />
        <activity
            android:name=".activities.RegisterUser"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.OverviewActivity" />
        <activity
            android:name=".activities.Login"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>